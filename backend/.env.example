PORT=5000
CLIENT_ORIGIN=http://localhost:5173

# --- AI chatbot ---
# Get a free key at https://console.groq.com (fast + generous free tier).
# Swap for OPENAI_API_KEY + the OpenAI SDK if you'd rather use OpenAI/Claude.
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.1-8b-instant

# --- Contact form email (Gmail app password or any SMTP provider) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=YOUR NAME
SMTP_PASS=YOUR PASSWORD
CONTACT_RECEIVER=shan.officialtech@gmail.com
